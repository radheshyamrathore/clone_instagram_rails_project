

 <h1><%= @post.body %></h1> 
<p><%= @post.post_type %></p>
<p><%= @post.user_id %></p>
<p><%= @user.user_name%></p>
<ul>
  <li><%= link_to "Destroy", post_path(@post), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure for delete post?"
                  } %></li>
</ul>
